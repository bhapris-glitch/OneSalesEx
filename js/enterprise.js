const enterpriseForm=document.getElementById('enterpriseForm');enterpriseForm?.addEventListener('submit',async e=>{e.preventDefault();const status=document.getElementById('enterpriseStatus');status.textContent='Sending your consultation request…';const body=Object.fromEntries(new FormData(enterpriseForm));try{const r=await fetch(`${window.LAYBOKA_API||''}/api/enterprise`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const data=await r.json();if(!r.ok)throw new Error(data.error||'Unable to submit request');const recipient=enterpriseForm.dataset.salesEmail||'sales@layboka.ai';const subject=`Enterprise consultation — ${body.company}`;const message=Object.entries(body).map(([key,value])=>`${key}: ${value}`).join('\n');const gmailUrl=`https://mail.google.com/mail/?view=cm&fs=1&tf=1&to=${encodeURIComponent(recipient)}&su=${encodeURIComponent(subject)}&body=${encodeURIComponent(message)}`;status.innerHTML=`${data.message||'Thanks — our enterprise team will contact you within 24 hours.'} <a href="${gmailUrl}" target="_blank" rel="noopener">Open Gmail</a>`;window.open(gmailUrl,'_blank','noopener');enterpriseForm.reset()}catch(error){status.textContent=error.message}});
